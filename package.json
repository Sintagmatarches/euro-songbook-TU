{
  "scripts": {
    "i18n:check": "node scripts/check-i18n.mjs",
    "i18n:draft:check": "node scripts/check-draft-ui-i18n.mjs",
    "test:search": "node --test tests/song-search.test.mjs",
    "build": "npx wrangler pages functions build functions --outdir .wrangler-build",
    "check": "npm run i18n:check && npm run i18n:draft:check && npm run test:search && npm run build"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0"
  }
}
